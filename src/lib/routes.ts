export const authenticationRoutes = {
    register: '/register',
    login: '/login'
}

const basicRoutes = {
    home: '/',
    profile: '/profile',
    aboutUs: '/about',
    services: '/services',
    explore: '/explore',
}

export default basicRoutes