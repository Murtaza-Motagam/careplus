export const authenticationRoutes = {
    register: '/register',
    login: '/login'
}

const basicRoutes = {
    home: '/',
    aboutUs: '/about',
    consultation: '/consultation',
    services: '/services',
    explore: '/explore',
}

export default basicRoutes